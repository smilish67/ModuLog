export interface GradioClientOptions {
    timeout?: number;
}
export declare class GradioClient<M extends Record<number, [any[], any[]]> = {}> {
    endpoint: string;
    private options;
    constructor(endpoint: string, options?: GradioClientOptions);
    request<I extends number & keyof M>(i: I, ...args: M[I][0]): Promise<M[I][1]>;
}
